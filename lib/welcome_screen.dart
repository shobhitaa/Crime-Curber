import 'package:flutter/material.dart';
import 'package:crime_curber/welcome_body.dart';

class WelcomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: WelcomeBody()
    );
  }
}
